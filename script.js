document.addEventListener('DOMContentLoaded', () => {\n  const dataDiv = document.getElementById('data');\n\n  // Fetch and display items\n  function loadItems() {\n    fetch('/api/items')\n      .then(res => res.json())\n      .then(items => {\n        dataDiv.innerHTML = '<ul>' + items.map(i => `<li>${i}</li>`).join('') + '</ul>';\n      })\n      .catch(err => {\n        console.error('Error fetching items:', err);\n        dataDiv.textContent = 'Failed to load items.';\n      });\n  }\n\n  // Example: add a new item\n  function addItem(name) {\n    fetch('/api/items', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ name })\n    })\n      .then(res => res.json())\n      .then(result => {\n        console.log('Item added:', result);\n        loadItems();\n      })\n      .catch(err => console.error('Error adding item:', err));\n  }\n\n  // Initial load\n  loadItems();\n\n  // For demo purposes, add a sample item after 2 seconds\n  setTimeout(() => addItem('Sample Item'), 2000);\n});