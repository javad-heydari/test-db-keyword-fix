const express=require('express');const app=express();const PORT=process.env.PORT||3000;let items=[];app.use(express.json());app.use(express.static(__dirname));app.get('/api/items',(req,res)=>{res.json(items);});app.post('/api/items',(req,res)=>{const {item}=req.body;if(item){items.push(item);res.status(201).json({message:'Added'});}else{res.status(400).json({error:'No item'});} });app.listen(PORT,()=>{console.log(`Server running on port ${PORT}`);});